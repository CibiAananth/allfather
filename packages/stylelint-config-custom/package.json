{
  "name": "stylelint-config-custom",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "format": "prettier . --write --ignore-unknown --loglevel warn --ignore-path .gitignore --ignore-path .prettierignore"
  },
  "dependencies": {
    "stylelint-config-standard": "^31.0.0",
    "stylelint-config-tailwindcss": "^0.0.7",
    "stylelint-scss": "^4.5.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
